import React, { Component } from 'react'
import PropTypes from 'prop-types'

let baseUrl=''
export default class LeftMenuPanel extends Component {   
     render() {
        // console.log("baseUrl",baseUrl)
        return (
            <div>
                <img src="./icons/docs-pile.svg"/>
            </div>
        )
    }
}
